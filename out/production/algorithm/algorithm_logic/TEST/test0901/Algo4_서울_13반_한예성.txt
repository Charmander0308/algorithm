Array 
- 임의 접근 시 인덱스 기반이기 때문에 시간복잡도가 O(1)이라 매우 빠르다.
- 중간 삽입, 삭제는 인덱스 기반이라 최악의 경우 전체수정이 이루어지기 때문에 시간 복잡도가 O(N)이라 상대적으로 느리다.
- 큐 기반의 메모리

LinkedList
- 임의 접근 시 노드 기반이기 때문에 조회를 통한 접근이 이루어져서 시간복잡도가 O(N)이라 상대적으로 느리다.
- 중간 삽입, 삭제는 연결 주소에 대한 변화만 이루어지므로 시간복잡도가 O(1)이라 매우 빠르다.
- 노드 기반의 메모리

